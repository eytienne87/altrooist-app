<%=render "components/hero"%>

<div class="explore-page justify-content-start d-flex container align-items-center">
  <i class="fas fa-search-location"></i>
  <h3>Montreal</h3>
</div>

<div class="container">
  <% @organizations.each do |organization| %>
    <%= render "components/organization-card", organization: organization%>
  <% end %>
</div>
